import Layout from "layout";

// =============================|| Contact_us ||============================= //

const Contact_us = () => <>Contact_us</>;

Contact_us.getLayout = function getLayout(page) {
  return <Layout>{page}</Layout>;
};

export default Contact_us;
