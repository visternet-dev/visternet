import Layout from "layout";

// =============================|| BLOG ||============================= //

const Blog = () => <>Blog</>;

Blog.getLayout = function getLayout(page) {
  return <Layout>{page}</Layout>;
};

export default Blog;
